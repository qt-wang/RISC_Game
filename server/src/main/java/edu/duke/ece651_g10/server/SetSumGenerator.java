package edu.duke.ece651_g10.server;

/**
 * Generate an Array of numbers whose sum is fixed.
 */
public interface SetSumGenerator {
    /**
     * Get an array of numbers, whose sum is fixed.
     * @return An array, whose sum is fixed.
     */
    public int[] get();
}
